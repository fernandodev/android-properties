<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:paddingBottom="@dimen/activity_vertical_margin"
                android:paddingLeft="@dimen/activity_horizontal_margin"
                android:paddingRight="@dimen/activity_horizontal_margin"
                android:paddingTop="@dimen/activity_vertical_margin"
                tools:context=".MainActivity">

  <TextView
    android:id="@+id/title"
    android:layout_centerHorizontal="true"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:text="Values"
    android:textSize="20sp"/>

  <TextView
    android:id="@+id/config_title"
    android:layout_marginTop="10dp"
    android:layout_below="@id/title"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:text="config.properties"
    android:textSize="16sp"/>

  <TextView
    android:id="@+id/values_for_config"
    android:text="..."
    android:layout_marginTop="5dp"
    android:layout_below="@+id/config_title"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"/>

  <TextView
    android:id="@+id/another_title"
    android:layout_marginTop="10dp"
    android:layout_below="@id/values_for_config"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:text="another.properties"
    android:textSize="16sp"/>

  <TextView
    android:id="@+id/values_for_another"
    android:text="..."
    android:layout_marginTop="5dp"
    android:layout_below="@+id/another_title"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"/>

</RelativeLayout>
